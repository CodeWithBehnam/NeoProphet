[project]
name = "neoprophet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
[tool.uv]
cache-dir = "./.uv_cache"

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
